export interface Library {
  id: string;
  title: string;
  subtitle: string;
  area: string;
  lat: number;
  lng: number;
  gradient: string;
  borderColor: string;
  accentColor: string;
  icon: string;
  hasBooking: boolean;
}
